/* Selector por ETIQUETA: afecta a TODOS los <h1> de la página */
h1 {
    color: #2c3e50;
    font-size: 2rem;
}

/* Selector por ETIQUETA: afecta a TODOS los <p>, sin importar clase o id */
p {
    font-size: 1rem;
    color: #333;
}

/* Selector por CLASE (con punto ".") : afecta solo a elementos con class="destacado" */
.destacado {
    color: #e67e22;
    font-size: 1.2rem;
    font-weight: bold;
}

/* Selector por ID (con gato "#") : afecta solo al único elemento con id="aviso" */
#aviso {
    color: #c0392b;
    font-size: 1.1rem;
    background-color: #fdecea;
    padding: 8px;
}
      
